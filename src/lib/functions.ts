export const myAdd: (x: number, y: number) => number = function(
  x: number,
  y: number
): number {
  return x + y;
};

// export function myAdd(x: number, y: number): number {
//   return x + y;
// }